<h1>Showing: <%= @task.title %></h1>

<p><%= @task.title %></p>
<p><%= @task.details %></p>
<p>Task status: <%= @task.completed ? "Completed" : "Todo" %></p>

<%= link_to "Edit task", edit_task_path(@task) %>
<%= link_to "Go to index", tasks_path %>